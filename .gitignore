node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Spreadsheet working files — local-only economic models / scratchpads.
# Track lockfiles too so an open Excel doc never sneaks into a commit.
*.xlsx
.~lock.*.xlsx#

# Local plugin/experimental workspaces, not part of the app
story-mode-plugin/
story-mode-toolkit.plugin

# brains-trust skill working dir (prompts + raw model responses from audits).
# Synthesis lives in docs/specs/*.md; raw artifacts stay local.
.jez/

# Environment variables (keep your API keys safe!)
.env
.env.local
.env.*.local